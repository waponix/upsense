import {Entity, Column, ManyToOne, OneToMany, ManyToMany, JoinTable, JoinColumn} from 'typeorm';import { BaseEntity } from './BaseEntity';import { User } from './User';import { Hub } from './Hub';import {Company} from "./Company";@Entity({ name: 'zones' })export class Zone extends BaseEntity{    @Column({ type: 'text', nullable: false, default: '' })    name!: string;    @ManyToMany(() => User, user => user.zones, { onDelete: 'NO ACTION', cascade: ['insert', 'update' ]})    @JoinTable({        name: 'user_zones',        joinColumn: {            name: 'zone',            referencedColumnName: 'id'        },        inverseJoinColumn: {            name: 'user',            referencedColumnName: 'id'        }    })    users!: User[];    @ManyToOne(() => Company, company => company.zones, { onDelete: 'CASCADE', cascade: ['insert', 'update']})    @JoinColumn({name: 'company_id'})    company!: Company;    @OneToMany(() => Hub, hub => hub.zone, {cascade: ['insert', 'update'], onDelete: 'SET NULL'})    hubs!: Hub[];    serialize()    {        let serialized: any = {            id: this.id,            name: this.name,            updatedAt: this.updatedAt,            createdAt: this.createdAt        };        if (this.users) {            serialized.users = this.users.map(user => user.serialize());        }        if (this.company) {            serialized.company = this.company.serialize();        }        return serialized;    }}